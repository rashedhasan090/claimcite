\section{Results}
We find that accuracy improves by 4 points over the prior art~\citep{lee2023}.

Later, far from any marker, the model outperforms the ablated variant across all five folds on held-out data.
